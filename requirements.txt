# Direct dependencies only — transitive packages are resolved by pip.
# Minimum versions reflect what the codebase was developed and tested against.

# API
fastapi>=0.100
uvicorn>=0.20
pydantic>=2.0

# Scraping
requests>=2.28
beautifulsoup4>=4.12

# Retrieval
chromadb>=1.5
numpy>=1.24
langchain-text-splitters>=0.3

# Gemini (embeddings + generation)
google-generativeai>=0.8

# Testing
pytest>=8.0
pytest-cov>=4.0
